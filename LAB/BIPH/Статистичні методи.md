# 0.1
Встановити пакет
	install.packages(“назва_пакета”)
підключити відповідну бібліотеку
	library(назва_пакета)

Дані
- числові
- текстові
- логічні

## Вибрати дані
```R
gaitndg$GAIT #- зміна
gaitndg[4]# - номер стовпчика
gaitndg[11:20, 3:4]# - вибере з 3 і 4 стопчиків датафрейму gaitndg рядки з 11 по 20 включно
gaitndg[which(g$GR=='CON'),]# вибрати лише частину спостережень за певною умовою.
```
[більше способів](https://www.datacamp.com/doc/r/subset)
Selecting Variables
```R
myvars <- c("v1", "v2", "v3")
newdata <- mydata[myvars]

yvars <- paste("v", 1:3, sep="")
newdata <- mydata[myvars]
```
Excluding Variables
```R
# exclude variables v1, v2, v3
myvars <- names(mydata) %in% c("v1", "v2", "v3")
newdata <- mydata[!myvars]

# exclude 3rd and 5th variable - як це працює

newdata <- mydata[c(-3,-5)]

# delete variables v3 and v5
mydata$v3 <- mydata$v5 <- NULL
```
Selecting Observations
```R
# first 5 observations
newdata <- mydata[1:5,]

# based on variable values
newdata <- mydata[ which(mydata$gender=='F'
& mydata$age > 65), ]

# or
attach(mydata)
newdata <- mydata[which(gender=='F' & age > 65),]
detach(mydata)
```
Selection using the Subset Function
```R
# using subset function
newdata <- subset(mydata, age >= 20 | age < 10,
select=c(ID, Weight))
```

## Фактори
- = categorical variables
- контроль, 1мкМ, 10 мкМ
- Спеціальний тип колонки
```R
# якщо дані вже є в таблиці
g$GROUP <- as.factor(g$GROUP)
# Фактор можна створити і на основі чисельних даних за допомогою функції ifelse()
mydataframe$f <- ifelse(gait_mat$age < 100, "Young", "Old")
mydataframe$factor <- ifelse(mydataframe$param < 100, "f1", ifelse(mydataframe$param < 200, "f2", "f3"))

mydataframet$f <- as.factor(mydataframet$f)

# якщо дані впорядковані то
f <- factor(mydataframe$dose, ordered = TRUE, levels = c("dose1", "dose2", "dose3"))

```
interaction() - щоб поєднати два categorical variables (factors)

Перелік найбільш потужних та популярних пакетів для роботи з даними:
dplyr - потужний пакет для складних маніпуляцій із даними
forcats - пакет для роботи із факторами
stringr - пакет для роботи з текстовими даними
 [[R for Data Science]](https://r4ds.had.co.nz/index.html)


## Описова статистика

У наукових статтях ви неодмінно бачили, як наводяться описи даних. Це може бути таблиця або пригадування певних відомостей просто у тексті. Кілька азбучних істин:
	нормальні дані описуються середнім арифметичним та середньоквадратичним відхиленням
	ненормальні - медіаною та квартилями
	**==розрядність==** усіх елементів має співпадати (10.22 ± 2.10 правильно; 10.22 ± 2.1 неправильно)
	не забуваємо зазначати ==розмірність== - цифри самі по собі сенсу не мають

 sapply( )
```R
 # get means for variables in data frame mydata
# excluding missing values

sapply(mydata, mean, na.rm=TRUE)
```
Possible functions used in sapply include mean, sd, var, min, max, median, range, and quantile.

summary()
```R
# mean,median,25th and 75th quartiles,min,max
summary(mydata)

# Tukey min,lower-hinge, median,upper-hinge,max
fivenum(x)
```

Hmisc
```R
library(Hmisc)
describe(mydata)
# n, nmiss, unique, mean, 5,10,25,50,75,90,95th percentiles
# 5 lowest and 5 highest scores
```

Pastecs
```R
library(pastecs)
stat.desc(mydata)

# nbr.val, nbr.null, nbr.na, min max, range, sum,
#
median, mean, SE.mean, CI.mean, var, std.dev, coef.var
```
Psych
```R
library(psych)
describe(mydata)
# item name ,item number, nvalid,mean, sd,
#median, mad, min, max, skew, kurtosis, se

# A simple way of generating summary statistics by grouping variable is available in the psych package.
library(psych)
describe.by(mydata, group,...)

```
середнє арифметичне (або просто середнє значення)	M, μ	
- сума усіх значень, поділена на кількість елементів у вибірці	
- характеризує центральну тенденцію ряду даних за умови нормальності розподілу вибірки

медіана	Me	
- значення, яке ділить вибірку на дві рівні частини; половина елементів вибірки має значення, менше за медіану, інша половина - більше	
- характеризує центральну тенденцію ряду даних, в тому числі і не нормально розподілених

мода	Mo	
- елемент вибірки, який зустрічається найчастіше	
- характеризує центральну тенденцію ряду даних, в якому певні значення зустрічаються з великою кількістю повторів

середньо-квадратичне відхилення	σ	
- корінь з суми квадратів різниці значень кожного елементу вибірки та середнього арифметичного, поділеної на число елементів у вибірці	
- характеризує розкид даних відносно середнього арифметичного; застосовується для опису нормально розподілених виборок; має таку ж розмірність, що і аналізована величина (наприклад, см)

дисперсія	σ2 або D	
- квадрат середньоквадратичного відхилення	
- характеризує розкид даних відносно середнього арифметичного; застосовується в статистичних тестах для порівняння розкидів різних наборів нормально розподілених даних; має квадратичну розмірність аналізованої величини (наприклад, см2)

коефіцієнт варіації	CV	
- σ, поділене на середнє арифметичне	
- те ж, що і σ, але нормоване, тобто не залежить від розмірності аналізованої величини; дозволяє порівнювати розкиди різних параметрів (наприклад, зросту і ваги групи людей); застосовується для нормально розподілених даних

похибка середнього або стандартна похибка	m	
- σ, поділене на корінь з числа елементів вибірки	
- характеризує межі, в яких відносно середнього вибіркового може міститися середнє генеральне; застосовується для нормально розподілених даних

мінімум; максимум	min; max	
- мінімальне і максимальне значення елементів вибірки	
- певною мірою характеризує розкид даних не залежно від характеру розподілу; дуже чутливі до "викидів" показники

квартилі: нижній і верхній	LQ, HQ	
- значення, що відсікають нижні (LQ) та верхні (HQ) 25% частини вибірки	
- характеризують розкид даних, не залежно від характеру розподілу даних, є більш коректними показниками в порівнянні з мінімумом та максимумом

коефіцієнт асиметрії		
- є мірою симетричності розподілу експериментальних даних відносно центрального значення (M); застосовується разом з ексцесом для оцінки нормальності розподілу вибірки

ексцес		
- є мірою крутизни графіку розподілу експериментальних даних; застосовується разом з асиметрією для оцінки нормальності розподілу вибірки


Version 
```
sessioninfo::session_info(c("tidyverse"))
```



# 01. Пара
Fisher's principles:
- Comparison - control/treatment
- Randomization - where is posible
- Statistical replication - duplication?
- Blocking 
	- ?
- Orthogonality - all except the key procedure
- Factorial experiments

Data type 
- Categorical 
	- Nominal -  gender, color eyes 
	- Ordinal -compare but without ratio comparison -  test score, 
- Numerical 
	- Interval - temperature, 
	- Ratio - length, weight

R data type 
vector 
factor
matrix
List
DataFrame


Параметричні та непараметричні данні
- параметричні відповідає параметром нормального розподілу


Середні значення
міра центральної тенденції 
мода
медіана 
середнє арефметичне

Коли ми починаємо використовувати непараметричні методи?


# 02. Перевірка даних на нормальність
Гістограма - швидкий проте не точний метод

[[QQ plot]]  - за його допомогою порівнюються наші дані (емпіричні) та теоричні (нормальні). На нормальний розподіл наших даних вказує лінійність.

Тестів на визначення нормальності розподілу існує чимало: Шапіро-Вілка, Лілієфорса, Колмогорова-Смірнова, Андерсона-Дарлінга та інші. 
❗Тест Шапіро-Вілка добре працює при невеликих обсягах вибірок (<50) - за цих умов він найпотужніший поміж усіх. Проте, якщо число елементів становить більше 50, він стає занадто чутливим до невеликих відхилень від нормальності (а при перевищенні 5000 функція взагалі не працює). 
Тому для вибірок з n>50 варто використовувати QQ-plot. Але якщо ви аналізуєте чимало вибірок, графічна перевірка може бути дуже витратною за часом, і тоді краще скористатися якимось формальним критерієм.
[[Shapiro–Wilk test]]




